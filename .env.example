# Postgres connection string.
# Matches the credentials in docker-compose.yml for local development.
DATABASE_URL=postgres://payload:payload@127.0.0.1:5432/aiforgoodwebsite

# Long random string used to sign tokens. Generate one with:
#   openssl rand -hex 32
PAYLOAD_SECRET=YOUR_SECRET_HERE
